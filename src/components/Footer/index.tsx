import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer>
      <div className={`${styles.wrapper} container`}>
        <div>addf</div>
      </div>
    </footer>
  );
};

export default Footer;
